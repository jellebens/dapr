apiVersion: v1
kind: Service
metadata:
  name: {{ template "dapractors.fullname" . }}
  labels:
    app: {{ template "dapractors.name" . }}
    chart: {{ template "dapractors.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "dapractors.name" . }}
    release: {{ .Release.Name }}
